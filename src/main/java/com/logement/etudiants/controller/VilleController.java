package com.logement.etudiants.controller;

import com.logement.etudiants.dto.response.VilleResponse;
//import com.logement.etudiants.service.VilleService;
import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;
/*
@RestController
@RequestMapping("/villes")
@RequiredArgsConstructor
@Tag(name="Villes")
public class VilleController {

    private final VilleService villeService;

    @GetMapping
    public ResponseEntity<List<VilleResponse>> list(){
        return ResponseEntity.ok(villeService.getAll());
    }
}

 */

